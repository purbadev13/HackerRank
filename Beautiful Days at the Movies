def beautifulDays(i, j, k):
    count=0
    for num in range (i,j+1):
        num1=str(num)
        num2=int(num1[::-1])
        num3=abs(num-num2)
        if num3%k==0:
            count+=1
        else:
            continue
    return count       
