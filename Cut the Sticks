def cutTheSticks(arr):
    result = list()
    arr.sort(reverse=True)
    while len(arr) > 0:
        result.append(len(arr))
        min = arr.pop()
        while len(arr) > 0 and arr[-1] <= min:
            arr.pop()     
            
    return result       
