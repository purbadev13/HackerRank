def cutTheSticks(arr):
    result = list()
    arr.sort(reverse=True)
    while len(arr) > 0:
        result.append(len(arr))
        block_cut = arr.pop()
        while len(arr) > 0 and arr[-1] <= block_cut:
            arr.pop()     
            
    return result       
